/** localStorage key for life-first photo journal — see docs/first-moments-ui-design.md */
export const FIRST_MOMENTS_STORAGE_KEY = 'yarntrailsFirstMoments';

/** Max upload size per file (bytes) for localStorage MVP */
export const FIRST_MOMENT_MAX_FILE_BYTES = 2 * 1024 * 1024;
